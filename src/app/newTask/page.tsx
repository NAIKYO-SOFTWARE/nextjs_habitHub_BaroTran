'use client';

import { Button } from '@/src/component/BaseUI/ButtonMUI';

function NewTask() {
    return (
        <Button variant="outlined" pill>
            On
        </Button>
    );
}

export default NewTask;
